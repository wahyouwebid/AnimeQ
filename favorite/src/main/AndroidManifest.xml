<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="web.id.wahyou.favorite">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_favorite">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".manga.MangaFavoriteActivity" />
        <activity android:name=".anime.AnimeFavoriteActivity" />
    </application>

</manifest>